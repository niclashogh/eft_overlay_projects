namespace efto_model.Models.Enums
{
    public enum SQLPropertyTypes
    {
        INTEGER,
        DOUBLE,
        VARCHAR, // [A-Z & a-z]
        nVARCHAR, // All char
        TEXT,
        TINTYINT, // ?
        BIT // Bool
    }
}
