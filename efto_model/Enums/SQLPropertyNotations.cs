namespace efto_model.Models.Enums
{
    public enum SQLPropertyNotations
    {
        PrimaryKeyId,
        PrimaryKeyName,
        ForeignKey,
        Unique,
        NotNull,
        Nullable
    }
}
